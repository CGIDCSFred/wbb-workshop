streamlit>=1.32.0
pandas>=2.0.0
anthropic>=0.108.0
# Makes Python trust the OS (Windows) certificate store, so the Anthropic SDK
# works behind corporate TLS-inspection proxies (e.g. Zscaler). Without it the
# SDK fails with CERTIFICATE_VERIFY_FAILED on such networks.
pip-system-certs>=4.0
